@model IEnumerable<Diplom.Models.Events>


@*<div class="flex-container">
    <div class="first">
        @foreach (var item in Model)
        {
            <div class="info">
                <div class="info-text">
                    <p>
                        @item.Name<br />
                        @item.short_description<br />


                        @item.created_by<br />
                        @item.start_date<br />
                        <br />

                        <a asp-action="Details" asp-controller="Home" asp-route-id="@item.Id" class="btn btn-primary">Подробнее</a>
                        <a asp-action="Edit" asp-controller="Home" asp-route-id="@item.Id" class="btn btn-primary">Изменить</a>
                        <a asp-action="Delete" asp-controller="Home" asp-route-id="@item.Id" class="btn btn-danger">Удалить</a>


                    </p>
                </div>

                <div class="img">
                    <img class="img-admin" src="~/img/events/@item.img_one">
                </div>
            </div>
            <div class="empty"></div>
        }

    </div>

</div>*@



<nav class="navbar navbar-expand-lg shadow navbar-light bg-light mb-3">
    <div class="container-fluid">
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a asp-action="AdminEventsList" asp-controller="Home" asp-route-all="1" class="btn btn-primary">Все</a>
                </li>
                <li class="nav-item">
                    <a asp-action="AdminEventsList" asp-controller="Home" asp-route-paid="1" class="btn btn-primary">Премиум</a>
                </li>
                <li class="nav-item">
                    <a asp-action="AdminEventsList" asp-controller="Home" asp-route-nopaid="1" class="btn btn-primary">Обычные</a>
                </li>
                <li class="nav-item">
                    <a asp-action="AdminEventsList" asp-controller="Home" asp-route-close="1" class="btn btn-primary">Закрытые</a>
                </li>

                @foreach (var cat in @ViewBag.catigoryItems)
                {
                    <li class="nav-item">
                        <a class="nav-link active" asp-action="AdminEventsList" asp-controller="Home" asp-route-viewcatigory="@cat.Id">@cat.name</a>
                    </li>
                }
            </ul>
        </div>
    </div>
</nav>




<div>
    <a asp-action="Create" asp-controller="Home" class="btn btn-primary">Создать мероприятие</a>
</div>
<br />
<div class="row">

    
        @foreach (var item in Model)
        {
        <div class="col" style="margin-bottom:2%;">
            <div class="card mx-auto" style="width: 340px; height:230px;">

                <img style="object-fit: cover; width: 100%; height: 100%; filter: blur(2px);" class="card-img" src="~/img/events/@item.img_one" alt="...">

            <div class="card-img-overlay" >
                <!-- Текстовое содержимое карточки -->
                    <p style="color:white";><b>
                        Название: @item.Name<br />
                        Статус: @item.status<br />
                        Платный: @item.paid<br />
                        Создатель: @item.created_by<br />
                        Начало: @item.start_date<br />
                        Дата создания: @item.created_date<br />
                        
                        <br />

                        <a asp-action="Details" asp-controller="Home" asp-route-id="@item.Id" class="btn btn-outline-primary" style="color:white">Подробнее</a>
                        <a asp-action="AdminEditEvents" asp-controller="Home" asp-route-id="@item.Id" class="btn btn-outline-primary" style="color:white">Изменить</a>
                        <a asp-action="AdminDeleteEvents" asp-controller="Home" asp-route-id="@item.Id" class="btn btn-outline-danger" style="color:white">Удалить</a>

                        </b>
                    </p>
            </div>
        </div>
        
    </div>
    }
</div>


